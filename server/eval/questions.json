{
  "repo": "test-fixtures/sample-mern",
  "note": "Golden questions for the sample fixture. `expected` lists unit ids, any one of which counts as a hit — several are often equally correct answers to the same question. Add a matching file for your own repository and point `code:eval` at it.",
  "questions": [
    { "q": "where are all users loaded from the database", "type": "conceptual", "expected": ["shop:services/userService.js#findAll"] },
    { "q": "where is a new user saved to the database", "type": "conceptual", "expected": ["shop:services/userService.js#createUser"] },
    { "q": "how do I fetch a single user by id", "type": "conceptual", "expected": ["shop:services/userService.js#getUserById"] },
    { "q": "findAll", "type": "symbol", "expected": ["shop:services/userService.js#findAll"] },
    { "q": "getUserById", "type": "symbol", "expected": ["shop:services/userService.js#getUserById"] },
    { "q": "createUser", "type": "symbol", "expected": ["shop:services/userService.js#createUser", "shop:controllers/userController.js#createUser"] },
    { "q": "useUsers", "type": "symbol", "expected": ["shop:src/hooks/useUsers.ts#useUsers"] },
    { "q": "UserCard", "type": "symbol", "expected": ["shop:src/components/UserCard.tsx#UserCard"] },
    { "q": "UsersPage", "type": "symbol", "expected": ["shop:src/pages/UsersPage.tsx#UsersPage"] },
    { "q": "listUsers", "type": "symbol", "expected": ["shop:controllers/userController.js#listUsers"] },
    { "q": "what happens when the users page loads", "type": "flow", "expected": ["shop:src/hooks/useUsers.ts#useUsers", "shop:routes/users.js#route:GET /api/users", "shop:controllers/userController.js#listUsers"] },
    { "q": "how does creating a user work end to end", "type": "flow", "expected": ["shop:routes/users.js#route:POST /api/users", "shop:controllers/userController.js#createUser", "shop:services/userService.js#createUser"] },
    { "q": "trace how the frontend gets its user data", "type": "flow", "expected": ["shop:src/hooks/useUsers.ts#useUsers", "shop:routes/users.js#route:GET /api/users"] },
    { "q": "walk me through rendering the list of users", "type": "flow", "expected": ["shop:src/pages/UsersPage.tsx#UsersPage", "shop:src/components/UserCard.tsx#UserCard"] },
    { "q": "what breaks if I change findAll", "type": "impact", "expected": ["shop:controllers/userController.js#listUsers", "shop:services/userService.js#findAll"] },
    { "q": "where is useUsers used", "type": "impact", "expected": ["shop:src/pages/UsersPage.tsx#UsersPage", "shop:src/hooks/useUsers.ts#useUsers"] },
    { "q": "who calls the user service", "type": "impact", "expected": ["shop:controllers/userController.js#listUsers", "shop:controllers/userController.js#createUser"] },
    { "q": "what depends on the User model", "type": "impact", "expected": ["shop:services/userService.js#findAll", "shop:models/User.js#<file>", "shop:services/userService.js#createUser"] },
    { "q": "which HTTP endpoints exist for users", "type": "conceptual", "expected": ["shop:routes/users.js#route:GET /api/users", "shop:routes/users.js#route:POST /api/users"] },
    { "q": "what is the POST endpoint for creating users", "type": "conceptual", "expected": ["shop:routes/users.js#route:POST /api/users"] },
    { "q": "where is the express server started", "type": "conceptual", "expected": ["shop:server.js#<file>"] },
    { "q": "how is the users router mounted", "type": "conceptual", "expected": ["shop:server.js#<file>", "shop:routes/users.js#<file>"] },
    { "q": "what shape does a user document have", "type": "conceptual", "expected": ["shop:models/User.js#<file>"] },
    { "q": "which component displays a single user", "type": "conceptual", "expected": ["shop:src/components/UserCard.tsx#UserCard"] },
    { "q": "where is the client side routing defined", "type": "conceptual", "expected": ["shop:src/App.tsx#App"] },
    { "q": "which page is shown at /users", "type": "conceptual", "expected": ["shop:src/pages/UsersPage.tsx#UsersPage", "shop:src/App.tsx#App"] },
    { "q": "what does the app use to make HTTP requests", "type": "conceptual", "expected": ["shop:src/hooks/useUsers.ts#useUsers"] },
    { "q": "which dependencies does this project use", "type": "config", "expected": ["shop:package.json#<file>"] },
    { "q": "how are import path aliases configured", "type": "config", "expected": ["shop:tsconfig.json#<file>"] },
    { "q": "what is the props type of the user card", "type": "conceptual", "expected": ["shop:src/components/UserCard.tsx#UserCardProps", "shop:src/components/UserCard.tsx#UserCard"] },
    { "q": "where does the controller send its JSON response", "type": "conceptual", "expected": ["shop:controllers/userController.js#listUsers", "shop:controllers/userController.js#createUser"] },
    { "q": "what mongoose schema is defined", "type": "conceptual", "expected": ["shop:models/User.js#<file>"] }
  ]
}
