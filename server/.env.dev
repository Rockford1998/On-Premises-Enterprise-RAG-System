NODE_ENV=dev
PORT="3000"
DATABASE_URL="postgresql://root:root@localhost:5432/poc"
MONGODB_URL="mongodb://root:root@127.0.0.1:27017/poc?authSource=admin"
DB_NAME="poc"
DB_USER="root"
DB_PASSWORD="root"
DB_HOST="localhost"
DB_PORT="5432"

OLLAMA_BASE_URL="http://localhost:11434"
BASE_MODEL="gpt-oss:120b-cloud" #mistral:latest | llama3.2:latest
TOOL_MODEL="gpt-oss:120b-cloud"
EMBEDDING_MODEL="nomic-embed-text"
TOP_K=4

# ---- Auth / session ----
# Secrets are required; the server refuses to start without them.
JWT_SECRET="_qOKIcjJsb2swTk25T1kwCLXVDRtSmI0rbH-lH2KIGBo0n31976gDIMWWIc4EY35"
JWT_REFRESH_SECRET="nbKotfMmaeYWsKBdznW8D-gVFND_8JgRZOGei609fOpTXzyjIrhfrZW3wpM3z6wu"
ACCESS_TOKEN_TTL=900          # 15 minutes, in seconds
REFRESH_TOKEN_TTL=604800      # 7 days, in seconds
MAX_SESSIONS_PER_USER=5
LOGIN_MAX_ATTEMPTS=10
LOGIN_WINDOW_SECONDS=900
REFRESH_COOKIE_NAME="rag_rt"
# Credentialed CORS cannot use "*". Comma-separate multiple origins.
CLIENT_ORIGIN="http://localhost:5173"

# generateEmbedding.ts reads EMBEDDING_MODEL; bot.controller.ts reads EMBED_MODEL.
EMBED_MODEL="nomic-embed-text"



# Credentialed CORS forbids "*". Comma-separate multiple origins.
CLIENT_ORIGIN="http://localhost:5173"

# ---- Google Drive knowledge-base sync (optional) ----
# Only needed if a bot connects a Google Drive source. Create an OAuth 2.0
# Client ID (Web application) in Google Cloud Console, add
# GOOGLE_OAUTH_REDIRECT_URI as an authorized redirect URI, and enable the
# Drive API for the project.
GOOGLE_OAUTH_CLIENT_ID=""
GOOGLE_OAUTH_CLIENT_SECRET=""
GOOGLE_OAUTH_REDIRECT_URI="http://localhost:3000/kb/connections/google/callback"

# 32-byte key (base64) used to encrypt stored Drive refresh tokens.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
CREDENTIALS_ENCRYPTION_KEY=""
